<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/base_toolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/white"
    app:contentInsetLeft="0dp"
    app:contentInsetStart="0dp">

    <TextView
        android:id="@+id/sms_main_quantity"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:drawablePadding="3dp"
        android:gravity="center_vertical"
        android:paddingLeft="13dp"
        android:textSize="14sp"
        android:background="@color/white"
        android:layout_weight="1"
        android:text="  "
        android:drawableLeft="@drawable/iv_back"/>

    <EditText
        android:id="@+id/sms_et_name"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:paddingLeft="8dp"
        android:textColor="@color/black"
        android:hint="@string/TMT_short_message_receiver"
        android:textSize="14sp"
        android:layout_weight="1"
        android:background="@drawable/custom_edittext_layout_sms"
        />

    <ImageView
        android:visibility="visible"
        android:id="@+id/sms_base_tv_toolbar_right"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:gravity="center"
        android:paddingLeft="0dp"
        android:paddingRight="17dp"
        android:src="@drawable/user" />

</android.support.v7.widget.Toolbar>